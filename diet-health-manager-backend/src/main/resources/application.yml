server:
  port: 8080

spring:
  application:
    name: diet-health-manager
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/diet_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456
  data:
    redis:
      host: localhost
      port: 6379
      database: 0

# MyBatis Plus ??
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.fanchenyi.diet.model.entity
  configuration:
    # ???????? [cite: 80]
    map-underscore-to-camel-case: true
    # ?? SQL ?? (??????)
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      # ?????? [cite: 948]
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0